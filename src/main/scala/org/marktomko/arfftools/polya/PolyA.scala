package org.marktomko.arfftools.polya

object PolyA {
  // these are the bases that we'll consider in most cases
  val bases = Set('A', 'C', 'G', 'T')

  // this describes the input data set that we have
  val sequenceLength = 206
  val hexamerStartIndex = 100
  val hexamerEndIndex = 106
}
